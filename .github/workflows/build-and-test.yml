name: Build and test

on:
  push:
    branches: 
      - features/**
      - releases/**
      - hotfix/**
      - develop
  pull_request:
    branches: 
      - develop
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Install dependencies
      run: dotnet restore ./src/AzureMapsControl.Components/AzureMapsControl.Components.csproj
    - name: Build
      run: dotnet build --configuration Release --no-restore ./src/AzureMapsControl.Components/AzureMapsControl.Components.csproj
    - name: Test
      run: dotnet test ./tests/AzureMapsControl.Components.Tests/AzureMapsControl.Component.Tests.csproj
